git add . && git commit -am "deleting Context.tsx and contextProvider.tsx - pushed to git as a reminder of the hard work I tried to put in but it turns out is the wrong solution this whole time -_-" && git push
